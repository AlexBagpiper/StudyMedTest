# Локальная разработка и тесты. На сервере не использовать (конфликты при развёртывании).
# Установка: python scripts/setup_dev_venv.py (из корня проекта)
-r requirements.txt

# Development & Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
black==24.3.0
isort==5.13.2
flake8==7.0.0
mypy==1.8.0
bandit==1.7.7
# safety несовместим с pydantic 2.x; при необходимости: pip install safety в отдельном venv
