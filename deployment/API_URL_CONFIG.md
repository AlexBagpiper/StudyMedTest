# Настройка API URL

## Как работает конфигурация API URL

### В режиме разработки (Development)

Когда вы запускаете `npm run dev` в папке `frontend`:

1. **Vite dev server** запускается на `http://localhost:5173`
2. **Vite proxy** настроен в `frontend/vite.config.ts`:
   ```typescript
   proxy: {
     '/api': {
       target: 'http://127.0.0.1:8000',
       changeOrigin: true,
     }
   }
   ```
3. **API клиент** использует относительный путь `/api/v1`
4. **Результат**: запрос к `/api/v1/auth/login` → `http://127.0.0.1:8000/api/v1/auth/login`

**НЕ НУЖНО** устанавливать `VITE_API_URL` в `frontend/.env` для разработки!

### В production

#### Вариант 1: С Nginx proxy (как в docker-compose.yml)

**Структура:**
```
Browser → Nginx (порт 80/443)
           ├─ / → Frontend (статика из nginx контейнера)
           └─ /api/ → Backend (FastAPI на порту 8000)
```

**Конфигурация:**
- В `deployment/.env` установите: `VITE_API_URL=/api/v1` (или не устанавливайте - используется по умолчанию)
- Nginx автоматически проксирует `/api/*` на backend

**Плюсы:**
- ✅ Нет проблем с CORS
- ✅ Один домен для всего
- ✅ SSL сертификат только для одного домена

#### Вариант 2: Раздельные домены

**Структура:**
```
Browser → app.yourdomain.com (Frontend)
        → api.yourdomain.com (Backend API)
```

**Конфигурация:**
- В `deployment/.env` установите: `VITE_API_URL=https://api.yourdomain.com/api/v1`
- Настройте CORS в backend (в `backend/app/main.py`):
  ```python
  app.add_middleware(
      CORSMiddleware,
      allow_origins=["https://app.yourdomain.com"],
      allow_credentials=True,
      allow_methods=["*"],
      allow_headers=["*"],
  )
  ```

**Плюсы:**
- ✅ Можно масштабировать frontend и backend отдельно
- ✅ Можно использовать CDN для frontend

**Минусы:**
- ⚠️ Нужно настроить CORS
- ⚠️ Два SSL сертификата

## Как API URL передается в production build

1. В `deployment/.env` устанавливаете `VITE_API_URL=/api/v1`
2. В `docker-compose.yml` эта переменная передается как build arg:
   ```yaml
   frontend:
     build:
       args:
         VITE_API_URL: ${VITE_API_URL:-/api/v1}
   ```
3. В `frontend/Dockerfile` переменная используется при сборке:
   ```dockerfile
   ARG VITE_API_URL=/api/v1
   ENV VITE_API_URL=$VITE_API_URL
   RUN npm run build
   ```
4. Vite встраивает значение в JavaScript код во время `npm run build`
5. Результат: в собранном `dist/` код содержит нужный URL

## Проверка конфигурации

### В разработке

1. Запустите backend: `cd backend && uvicorn app.main:app --reload`
2. Запустите frontend: `cd frontend && npm run dev`
3. Откройте браузер на `http://localhost:5173`
4. Откройте DevTools → Console
5. Вы НЕ должны видеть `🔧 API_BASE_URL:` в консоли (debug логи удалены)
6. Попробуйте войти - запросы должны идти на `/api/v1/auth/login`

### В production

1. Соберите проект: `docker-compose build`
2. Запустите: `docker-compose up -d`
3. Откройте браузер на `http://localhost` (или ваш домен)
4. Проверьте Network tab в DevTools - запросы должны идти на `/api/v1/*`

## Troubleshooting

### Проблема: 404 Not Found на API запросы

**Симптомы:** `POST http://localhost:8000/auth/login 404`

**Причина:** Frontend пытается обойти proxy

**Решение:**
1. Удалите `frontend/.env` (или закомментируйте `VITE_API_URL`)
2. Перезапустите dev server (Ctrl+C → `npm run dev`)
3. Жестко обновите браузер (Ctrl+Shift+R)

### Проблема: CORS errors в production

**Симптомы:** `Access-Control-Allow-Origin` ошибки

**Причина:** Frontend и Backend на разных доменах без CORS

**Решение:**
- Используйте Nginx proxy (вариант 1)
- ИЛИ настройте CORS в backend (см. выше)

### Проблема: Старый API URL после изменения .env

**Причина:** Vite встраивает URL во время сборки, не runtime

**Решение:**
1. Измените `VITE_API_URL` в `deployment/.env`
2. Пересоберите frontend: `docker-compose build frontend`
3. Перезапустите: `docker-compose up -d frontend`
