# CV-оценка графических аннотаций

## Обзор

Система автоматической оценки графических аннотаций студентов на основе Computer Vision алгоритмов.

## Реализованная функциональность

### ✅ Backend (Python)

#### 1. CV Service (`backend/app/services/cv_service.py`)

**Алгоритм IoU (Intersection over Union)**
- Сравнение полигонов студента с эталонными аннотациями
- Поддержка типов: rectangle, polygon, ellipse, point

**Метрики качества:**
- **Accuracy** - средний IoU по всем аннотациям
- **Completeness (Recall)** - доля найденных объектов
- **Precision** - доля правильных среди нарисованных

**Взвешенный балл:**
```python
total_score = (
    accuracy * 0.5 +      # 50% - точность попадания
    completeness * 0.3 +  # 30% - полнота выделения
    precision * 0.2       # 20% - отсутствие лишних
) * 100
```

#### 2. Celery Task (`backend/app/tasks/evaluation_tasks.py`)

**Асинхронная обработка:**
```python
@celery_app.task(name="evaluate_annotation_answer")
def evaluate_annotation_answer(answer_id: str)
```

- Выполняется в очереди `cv`
- Загружает эталонные данные из `question.reference_data`
- Сравнивает с `answer.annotation_data`
- Сохраняет результаты в `answer.evaluation` и `answer.score`

#### 3. Формат данных

**Эталонные аннотации (`question.reference_data`):**
```json
{
  "annotations": [
    {
      "id": "ref_ann_1",
      "label_id": "tumor",
      "type": "rectangle",
      "bbox": [100, 100, 150, 120]
    }
  ],
  "labels": [
    {
      "id": "tumor",
      "name": "Опухоль",
      "color": "#FF0000"
    }
  ]
}
```

**Результат оценки (`answer.evaluation`):**
```json
{
  "iou_scores": [0.739, 0.739],
  "accuracy": 0.739,
  "completeness": 1.0,
  "precision": 1.0,
  "evaluated_at": "2026-01-22T06:58:19.630174"
}
```

### ✅ Frontend (React + TypeScript)

#### 1. Страница просмотра результатов (`SubmissionReviewPage.tsx`)

**Для графических вопросов:**
- Отображение аннотаций студента (read-only режим)
- Карточка с CV-оценкой:
  - Итоговый балл
  - Accuracy с прогресс-баром (вес 50%)
  - Completeness с прогресс-баром (вес 30%)
  - Precision с прогресс-баром (вес 20%)
  - Детали IoU по каждому объекту

**Для текстовых вопросов:**
- Отображение текста ответа
- Заглушка с информацией об LLM-оценке (будет в следующей версии)

## Тестирование

### Тестовый скрипт (`backend/tests/test_cv_annotation_real.py`)

**5 сценариев проверки:**

1. **Идеальное совпадение** - копия эталона → 100 баллов
2. **Частичное совпадение** - смещение 20% → 83.33 балла
3. **Лишние аннотации** - снижение Precision → 90 баллов
4. **Пропущенные аннотации** - снижение Completeness → 85 баллов
5. **Реальный submission** - создание в БД + оценка → 86.96 балла

**Запуск:**
```bash
cd backend
python tests/test_cv_annotation_real.py
```

### Вспомогательный скрипт (`backend/tests/add_reference_annotations.py`)

Добавляет эталонные аннотации к графическим вопросам в БД.

**Запуск:**
```bash
# Обновить все графические вопросы
python tests/add_reference_annotations.py

# Обновить конкретный вопрос
python tests/add_reference_annotations.py <question_id>
```

## Результаты тестирования

```
✅ СЦЕНАРИЙ 1: Идеальное совпадение
   IoU: [1.0, 1.0]
   Accuracy: 1.000, Completeness: 1.000, Precision: 1.000
   🎯 Итоговый балл: 100.00/100

✅ СЦЕНАРИЙ 2: Смещение 20%
   IoU: [0.667, 0.667]
   Accuracy: 0.667, Completeness: 1.000, Precision: 1.000
   🎯 Итоговый балл: 83.33/100

✅ СЦЕНАРИЙ 3: Лишние аннотации
   Студент: 4 аннотации, Эталон: 2
   Completeness: 1.000, Precision: 0.500 ← снижение!
   🎯 Итоговый балл: 90.00/100

✅ СЦЕНАРИЙ 4: Пропущенные
   Студент: 1 аннотация, Эталон: 2
   Completeness: 0.500 ← снижение!, Precision: 1.000
   🎯 Итоговый балл: 85.00/100

✅ СЦЕНАРИЙ 5: Реальный submission
   Смещение 15%, сохранение в БД
   🎯 Итоговый балл: 86.96/100
```

## Архитектура

```
┌─────────────────────┐
│   Frontend (React)  │
│  AnnotationEditor   │
└──────────┬──────────┘
           │ annotation_data
           ▼
┌─────────────────────┐
│   API Endpoint      │
│ POST /submissions   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   Celery Worker     │
│   cv queue          │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   CV Service        │
│  - IoU calculation  │
│  - Metrics          │
│  - Scoring          │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   Database          │
│  answer.evaluation  │
│  answer.score       │
└─────────────────────┘
```

## Используемые библиотеки

- **shapely** - работа с геометрическими фигурами, расчёт IoU
- **numpy** - вычисления метрик
- **celery** - асинхронная обработка

## Формула итогового балла

```
Total Score = (0.5 × Accuracy + 0.3 × Completeness + 0.2 × Precision) × 100

где:
  Accuracy = среднее(IoU всех matched объектов)
  Completeness = найдено объектов / всего в эталоне
  Precision = правильных / всего нарисовано
```

## Будущие улучшения

1. Настраиваемые веса метрик (сейчас 50/30/20)
2. Порог IoU для считания match (сейчас > 0)
3. Hungarian algorithm для оптимального matching
4. Поддержка multi-class evaluation
5. Визуализация различий между эталоном и ответом
